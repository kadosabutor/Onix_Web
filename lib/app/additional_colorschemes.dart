import 'package:flutter/material.dart';

final ColorScheme greenScheme = ColorScheme.fromSeed(
  seedColor: Colors.green,
  brightness: Brightness.light,
);

final ColorScheme yellowScheme = ColorScheme.fromSeed(
  seedColor: Colors.yellow,
  brightness: Brightness.light,
);

final ColorScheme redScheme = ColorScheme.fromSeed(
  seedColor: Colors.red,
  brightness: Brightness.light,
);
